In order for the website to properly work,the url of the website must follow the example below:
"localhost/library"

The folder "library" which contains all the necessary files (such as index.php, etc.) must be located in the folder "htdocs",
path example: ../xampp/htdocs/library.